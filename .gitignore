# === SENSITIVE: financial PII — never commit ===
*.pdf
ledger/
logs/
config.json
.env
.env.*
!.env.example

# Data / model artifacts
*.csv
*.pkl
*.pt
*.ckpt
__pycache__/
*.pyc
.venv/
venv/

# Test artifacts + scratch. The session-end auto-commit stages untracked
# files, so anything not ignored here lands in history uninvited.
pytest-of-frank/
tmp/

# Public repo: these leak the live watchlist and machine config.
run_news.py
intraday_task.xml

# OS
.DS_Store
Thumbs.db

# Private reasoning notes + model output charts (reveal positions)
notes/
output/
*.png
graphify-out/
